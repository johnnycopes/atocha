import { coreSelectionTreeUtil } from './core-selection-tree-util';

describe('coreSelectionTreeUtil', () => {
  it('should work', () => {
    expect(coreSelectionTreeUtil()).toEqual('core-selection-tree-util');
  });
});
