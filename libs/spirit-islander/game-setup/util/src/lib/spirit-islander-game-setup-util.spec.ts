import { spiritIslanderGameSetupUtil } from './spirit-islander-game-setup-util';

describe('spiritIslanderGameSetupUtil', () => {
  it('should work', () => {
    expect(spiritIslanderGameSetupUtil()).toEqual(
      'spirit-islander-game-setup-util'
    );
  });
});
