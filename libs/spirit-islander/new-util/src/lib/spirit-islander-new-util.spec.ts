import { spiritIslanderNewUtil } from './spirit-islander-new-util';

describe('spiritIslanderNewUtil', () => {
  it('should work', () => {
    expect(spiritIslanderNewUtil()).toEqual('spirit-islander-new-util');
  });
});
