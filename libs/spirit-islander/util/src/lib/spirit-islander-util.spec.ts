import { spiritIslanderUtil } from './spirit-islander-util';

describe('spiritIslanderUtil', () => {
  it('should work', () => {
    expect(spiritIslanderUtil()).toEqual('spirit-islander-util');
  });
});
