import { spiritIslanderSettingsUtil } from './spirit-islander-settings-util';

describe('spiritIslanderSettingsUtil', () => {
  it('should work', () => {
    expect(spiritIslanderSettingsUtil()).toEqual(
      'spirit-islander-settings-util'
    );
  });
});
