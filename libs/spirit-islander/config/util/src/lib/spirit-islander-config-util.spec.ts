import { spiritIslanderConfigUtil } from './spirit-islander-config-util';

describe('spiritIslanderConfigUtil', () => {
  it('should work', () => {
    expect(spiritIslanderConfigUtil()).toEqual('spirit-islander-config-util');
  });
});
