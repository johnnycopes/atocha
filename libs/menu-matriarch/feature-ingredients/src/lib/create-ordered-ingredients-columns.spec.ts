import { createOrderedIngredientsColumns } from './create-ordered-ingredients-columns';

describe('createOrderedIngredientsColumns', () => {
  it('returns', () => {
    expect(createOrderedIngredientsColumns).toBeTruthy();
  });
});
